name: get_blocked_users
description: >-
  List the users you have blocked. Returns { blocked_users, count } where each user is a
  summary (id, name, username). Read-only — the companion to block_user and
  unblock_user.
fields:
  limit:
    description: Maximum number of blocked users to return (default 20).
